spring:
  jackson:
    serialization:
      indent_output: true
  datasource:
    url: jdbc:h2:file:./foo
    username: foo
    driver-class-name: org.h2.Driver
  h2:
    console:
      enabled: true
      path: /h2
  jpa:
    #properties:
    #  hibernate:
    #    dialect: org.hibernate.dialect.MySQL5Dialect
    hibernate:
      ddl-auto: create
      show-sql: true
  
logging:
  level:
    de:
      chris:
        fun: DEBUG
